package com.ismuted;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class IsMutedSpec extends NativeIsMutedSpec {
  IsMutedSpec(ReactApplicationContext context) {
    super(context);
  }
}
